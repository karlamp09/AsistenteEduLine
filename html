<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>EduLine — Inteligencia Artificial y Data Science</title>
  <meta name="description" content="Programa de Inteligencia Artificial y Data Science en modalidad Campus LMS. Curso grabado, autoestudio. Costo: 1800 soles.">
  <style>
    /* Paleta de colores */
    :root{
      --celeste: #6ECFF6;
      --azul: #0B63E5;
      --verde-agua: #2EE6C7;
      --blanco: #FFFFFF;
      --gris-oscuro: #0f1724;
      --gris-claro: #F3F6F9;
      --card-shadow: 0 8px 30px rgba(11,99,229,0.08);
      --radius: 12px;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }

    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%;background:linear-gradient(180deg,var(--gris-claro),#ffffff);color:var(--gris-oscuro)}
    a{color:inherit;text-decoration:none}

    .container{max-width:1120px;margin:0 auto;padding:32px}

    header{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:18px 0}
    .brand{display:flex;align-items:center;gap:12px}
    .logo{width:48px;height:48px;border-radius:10px;background:linear-gradient(135deg,var(--celeste),var(--azul));display:flex;align-items:center;justify-content:center;color:var(--blanco);font-weight:700;box-shadow:var(--card-shadow)}
    .brand h1{font-size:18px}
    nav{display:flex;gap:14px;align-items:center}
    .nav-link{padding:8px 12px;border-radius:8px;color:var(--gris-oscuro);font-weight:600}
    .nav-link.primary{background:linear-gradient(90deg,var(--celeste),var(--azul));color:var(--blanco)}

    .hero{display:grid;grid-template-columns:1fr 420px;gap:32px;align-items:center;padding:48px 0}
    .hero h2{font-size:36px;line-height:1.02;margin-bottom:10px}
    .hero p{color:#334155;margin-bottom:20px}
    .price-badge{display:inline-flex;gap:10px;align-items:center;padding:10px 14px;background:linear-gradient(90deg,var(--verde-agua),var(--celeste));border-radius:999px;color:var(--gris-oscuro);font-weight:700}
    .btn{padding:12px 18px;border-radius:10px;font-weight:700;border:none;cursor:pointer}
    .btn-primary{background:linear-gradient(90deg,var(--azul),var(--celeste));color:var(--blanco);box-shadow:var(--card-shadow)}
    .btn-ghost{background:transparent;border:2px solid rgba(11,99,229,0.12);color:var(--azul)}

    .card{background:var(--blanco);padding:20px;border-radius:16px;box-shadow:var(--card-shadow)}

    .courses{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:18px;margin:28px 0}

    footer{margin-top:48px;padding:24px 0;border-top:1px solid rgba(15,23,36,0.04)}
  </style>
</head>
<body>
  <header class="container">
    <div class="brand">
      <div class="logo">ED</div>
      <div>
        <h1>EduLine</h1>
        <div style="font-size:12px;color:#475569">Inteligencia Artificial & Data Science</div>
      </div>
    </div>
    <nav>
      <a class="nav-link">Inicio</a>
      <a class="nav-link">Temario</a>
      <a class="nav-link">Tutorías</a>
      <a class="nav-link">Soporte</a>
      <a class="nav-link primary" href="#inscripcion">Inscribirme</a>
    </nav>
  </header>

  <main class="container">
    <section class="hero">
      <div>
        <div class="price-badge">📘 Programa: IA & Data Science · 1800 soles</div>
        <h2>Aprende Inteligencia Artificial y Ciencia de Datos desde cero.</h2>
        <p>Accede a clases grabadas, notebooks interactivos y proyectos prácticos. Estudia a tu ritmo en el <strong>Campus LMS</strong>.</p>
        <button class="btn btn-primary" onclick="scrollToSection('inscripcion')">Inscribirme</button>
      </div>
    </section>

    <section id="inscripcion">
      <h3>Inscríbete ahora</h3>
      <form id="signupForm" class="card" onsubmit="handleSignup(event)">
        <label>Nombre completo</label>
        <input id="name" required placeholder="Tu nombre" style="width:100%;padding:10px;border-radius:8px;border:1px solid rgba(2,6,23,0.06);margin-bottom:10px">
        <label>Correo electrónico</label>
        <input id="email" type="email" required placeholder="tucorreo@ejemplo.com" style="width:100%;padding:10px;border-radius:8px;border:1px solid rgba(2,6,23,0.06);margin-bottom:10px">
        <button class="btn btn-primary" type="submit">Pagar S/ 1800</button>
      </form>
    </section>

    <footer>
      <strong>EduLine</strong> · Inteligencia Artificial y Data Science · S/ 1800
    </footer>
  </main>

  <!-- Integración Dialogflow Messenger -->
  <script src="https://www.gstatic.com/dialogflow-console/fast/messenger/bootstrap.js?v=1"></script>
  <df-messenger
    chat-title="PROMPTER-EduLine"
    agent-id="2f63beb6-83aa-491f-b960-4e2c770acdbd"
    language-code="es">
  </df-messenger>

  <script>
    function scrollToSection(id){
      const el=document.getElementById(id);
      if(el) el.scrollIntoView({behavior:'smooth'});
    }
    function handleSignup(e){
      e.preventDefault();
      const name=document.getElementById('name').value;
      const email=document.getElementById('email').value;
      alert(`Gracias ${name}! Te contactaremos en ${email}.`);
    }
  </script>
</body>
</html>
